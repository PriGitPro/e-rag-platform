{
  "name": "enterprise-rag-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Enterprise RAG Platform monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @erp/api-gateway build && npm run -w @erp/ingestion-worker build",
    "dev:gateway": "npm run -w @erp/api-gateway dev",
    "dev:worker": "npm run -w @erp/ingestion-worker dev",
    "test": "npm run -w @erp/api-gateway test",
    "lint": "npm run -w @erp/api-gateway lint && npm run -w @erp/ingestion-worker typecheck",
    "typecheck": "npm run -w @erp/api-gateway typecheck && npm run -w @erp/ingestion-worker typecheck"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
